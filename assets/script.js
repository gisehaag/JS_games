class Game {
	constructor(config) {
		// const configDefaults = {
		// }

		// this.config = Object.assign(configDefaults, config);


		this.displayTemplate();
		this.addEvents();
	}

}

let game = new Game();




















